import { ReverseString2Pipe } from './reverse-string2.pipe';

describe('ReverseString2Pipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseString2Pipe();
    expect(pipe).toBeTruthy();
  });
});
